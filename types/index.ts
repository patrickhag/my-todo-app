export type ParamType = { id: string }

export type TodoType = {
  id?: string
  text: string
  description?: string
  done: boolean
  message?: string
}

export type User = {
  id: string
  email: string
  name: string
}
