export type ParamType = { id: string }

export type TodoType = {
  id?: string
  text: string
  description?: string
  done: boolean
}

export type User = {
  id: string
  email: string
  name: string
}
